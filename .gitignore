# ── Build output ──────────────────────────────────────
target/

# ── Local config (contains secrets — never commit) ───
src/main/resources/application-local.properties

# ── H2 database files (no longer used) ───────────────
data/

# ── OS / IDE ──────────────────────────────────────────
.DS_Store
Thumbs.db
*.log
.idea/
.vscode/
*.iml
